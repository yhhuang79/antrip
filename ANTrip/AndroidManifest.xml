<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="tw.plash.antrip"
    android:versionCode="3"
    android:versionName="1.0" >

    <uses-sdk android:minSdkVersion="10" />

    <!-- CAMERA PERMISSIONS -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />

    <!-- SKYHOOK PERMISSIONS -->
    <!-- used to communicate with Skyhook's servers -->
	<uses-permission android:name="android.permission.INTERNET" />
	<!-- enables WiFi, if disabled, for the duration of a location request -->
	<uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
	<!-- used to obtain information about the WiFi environment -->
	<uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
	<!-- required by our basic location algorithm -->
	<uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
	<uses-permission android:name="android.permission.ACCESS_COARSE_UPDATES" />
	<uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
	<uses-permission android:name="android.permission.WAKE_LOCK" />
	<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
	<uses-permission android:name="android.permission.CONTROL_LOCATION_UPDATES" />
    
	
    <application
        android:icon="@drawable/ant_48"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.NoTitleBar" >
        <activity
            android:name=".ANTripActivity"
            android:label="@string/app_name" android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <service android:name="LocationService" android:process=":remote"></service>
        <service android:name="LocationServiceGPS" android:process=":remote"></service>
    </application>

</manifest>